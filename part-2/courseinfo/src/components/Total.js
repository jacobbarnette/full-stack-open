const Total = ({course}) => {
  course.forEach(element => {
  
  });
 /* let  newArr = [];
  let initialValue = 0;
  course.map(course => {
    newArr.push(course.exercises)
  })
  let newValue = newArr.reduce((previousValue, currentValue) => previousValue + currentValue, initialValue
  );*/
  return(
    <div>
      <p>Total Number of exercises 2</p>
    </div>
  )
}
export default Total